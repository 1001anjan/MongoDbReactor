pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script { // Enclose shell steps within a script block
                    sh './buildScript/build.sh'
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    sh './buildScript/test.sh'
                }
            }
        }

        // Define more stages as needed
    }
}